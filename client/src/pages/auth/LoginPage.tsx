import React from 'react';
import { Box, Typography } from '@mui/material';

const LoginPage: React.FC = () => {
  return (
    <Box p={3}>
      <Typography variant="h4">Страница входа</Typography>
      <Typography>Здесь будет форма входа в систему</Typography>
    </Box>
  );
};

export default LoginPage; 